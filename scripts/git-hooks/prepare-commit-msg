#!/usr/bin/env bash
# Remove agent/IDE co-author and attribution lines before the commit is finalized.
set -euo pipefail
msg_file="${1:?}"
sed -i \
  -e '/^Co-authored-by:/Id' \
  -e '/^Made-with:/Id' \
  -e '/^Made with \[/Id' \
  -e '/cursoragent@cursor\.com/Id' \
  "${msg_file}"
